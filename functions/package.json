{
  "name": "creathon-functions",
  "description": "Take Off — server-side work the app cannot be trusted with.",
  "private": true,
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "jsonwebtoken": "^9.0.2"
  }
}
