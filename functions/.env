# Non-secret function config. Committed on purpose: the JaaS tenant id is the
# first path segment of every join URL, so there is nothing here to hide.
# Anything that must stay secret goes to Secret Manager instead —
# `firebase functions:secrets:set` — never this file.
JAAS_APP_ID=vpaas-magic-cookie-27529844f95f4d0580ff59dc440d6a23
