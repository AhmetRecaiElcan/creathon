{
  "name": "creathon-rules-test",
  "description": "Take Off — Firestore rules tests. The invite gate is enforced here, not in the app.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project creathon-test \"node invites.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
